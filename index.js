require('dotenv').config();
const express = require('express');
const axios = require('axios');

const app = express();
app.use(express.json());

const PORT = process.env.PORT || 3000;

/* ===============================
   CONFIG
================================ */
const OPENAI_API_KEY = process.env.OPENAI_API_KEY;
const LINE_TOKEN = process.env.LINE_TOKEN;
const FINNHUB_API_KEY = process.env.FINNHUB_API_KEY;

/* ===============================
   SYSTEM PROMPT (บังคับตอบยาว)
================================ */
const SYSTEM_PROMPT = `
คุณคือ AI นักวิเคราะห์ตลาดของเพจ Signal Zeeker

กติกาการตอบ:
- เขียนเป็นย่อหน้ายาว ชัด ลึก แบบบทวิเคราะห์ข่าว
- อธิบายโดยสรุปภาพรวมแบบฉลาด
- ใช้ประโยคยาวได้
- แยกเป็นย่อหน้าใหญ่ชัดเจน
- ห้ามตอบกำกวม
- ถ้าไม่มีข้อมูลจริงให้บอกตรง ๆ
- สามารถยกตัวอย่างหุ้นได้ เพราะไม่ใช่คำแนะนำในการลงทุน เป็นการค้นหาเพื่อประกอบการตัดสินใจ 

โครงสร้างที่ต้องใช้:
1) ภาพรวม สรุปให้ครบถ้วนใน 150 ตัวอักษร
2) ปัจจัยสำคัญ สรุปให้ครบถ้วนใน 200 ตัวอักษร
3) ความเสี่ยง สรุปให้ครบถ้วนใน 200 ตัวอักษร
4) มุมมองตลาด สรุปให้ครบถ้วนใน 250 ตัวอักษร
5) สรุปเชิงวิเคราะห์ สรุปให้ครบถ้วนใน 100 ตัวอักษร
`;

/* ===============================
   Helper: ผ่าข้อความแบบกันหาย
================================ */
function splitForLine(text, maxLen = 900) {
  const messages = [];

  // แยกตามย่อหน้าใหญ่ก่อน
  const paragraphs = text
    .split(/\n{2,}/)
    .map(p => p.trim())
    .filter(Boolean);

  for (const p of paragraphs) {
    // ถ้าย่อหน้าสั้น → ใส่เลย
    if (p.length <= maxLen) {
      messages.push({ type: 'text', text: p });
    } 
    // ถ้าย่อหน้ายาว → ผ่าซ้ำ
    else {
      let start = 0;
      while (start < p.length) {
        messages.push({
          type: 'text',
          text: p.substring(start, start + maxLen)
        });
        start += maxLen;
      }
    }
  }

  // LINE แนะนำไม่เกิน 5–10 กล่อง
  return messages.slice(0, 8);
}

/* ===============================
   Finnhub: ราคาหุ้น
================================ */
async function getStockPrice(symbol) {
  const url = `https://finnhub.io/api/v1/quote?symbol=${symbol}&token=${FINNHUB_API_KEY}`;
  const res = await axios.get(url);
  return res.data;
}

/* ===============================
   OpenAI
================================ */
async function askOpenAI(prompt) {
  const res = await axios.post(
    'https://api.openai.com/v1/chat/completions',
    {
      model: 'gpt-3.5-turbo',
      messages: [
        { role: 'system', content: SYSTEM_PROMPT },
        { role: 'user', content: prompt }
      ],
      max_tokens: 800, // อนุญาตให้ตอบยาว
      temperature: 0.6
    },
    {
      headers: {
        Authorization: `Bearer ${OPENAI_API_KEY}`,
        'Content-Type': 'application/json'
      }
    }
  );

  return res.data.choices[0].message.content;
}

/* ===============================
   LINE WEBHOOK
================================ */
app.post('/webhook', async (req, res) => {
  try {
    const event = req.body.events?.[0];
    if (!event || event.type !== 'message') {
      return res.sendStatus(200);
    }

    const userText = event.message.text.trim();
    const symbolOnly = /^[A-Za-z]{1,6}$/.test(userText);

    let replyText = '';

    /* ===== พิมพ์ชื่อหุ้นอย่างเดียว ===== */
    if (symbolOnly) {
      const symbol = userText.toUpperCase();
      const price = await getStockPrice(symbol);

      replyText = `
📊 ${symbol} — ราคาปัจจุบัน
ราคาหุ้น ${symbol} ล่าสุดอยู่ที่ ${price.c} ดอลลาร์สหรัฐ โดยระหว่างวันทำจุดสูงสุดที่ ${price.h} ดอลลาร์ และจุดต่ำสุดที่ ${price.l} ดอลลาร์ ขณะที่ราคาปิดก่อนหน้าอยู่ที่ ${price.pc} ดอลลาร์ ซึ่งสะท้อนถึงระดับความผันผวนที่ยังอยู่ในระดับสูงตามลักษณะของหุ้นกลุ่ม Growth


🧠 ภาพรวมและปัจจัยสำคัญ
การเคลื่อนไหวของราคาหุ้นในช่วงนี้ยังถูกขับเคลื่อนด้วยความคาดหวังของนักลงทุนต่อผลประกอบการในอนาคต รวมถึงกระแสเงินทุนที่ไหลเข้าออกตามมุมมองเศรษฐกิจมหภาค อัตราดอกเบี้ย และ Sentiment ของตลาดโดยรวม


⚠️ ความเสี่ยงที่ต้องติดตาม
หุ้น ${symbol} เป็นหุ้นที่มีความอ่อนไหวต่อข่าวสูง ไม่ว่าจะเป็นข่าวผลประกอบการ การเปลี่ยนแปลงเชิงนโยบาย หรือท่าทีของนักลงทุนสถาบัน ซึ่งอาจทำให้ราคาเหวี่ยงแรงในระยะสั้นได้


📌 สรุปมุมมอง Signal Zeeker
ราคาที่เห็นเป็นเพียงปลายทางของการตัดสินใจ แต่สิ่งที่ต้องจับตาคือทิศทางของเงินทุนและพฤติกรรมของผู้เล่นรายใหญ่ เพราะตรงนั้นคือเกมจริงของตลาด
`;
    } 
    /* ===== คำถามทั่วไป ===== */
    else {
      replyText = await askOpenAI(userText);
    }

    /* ===== แยกเป็นหลายกล่อง ===== */
    const messages = splitForLine(replyText);

    await axios.post(
      'https://api.line.me/v2/bot/message/reply',
      {
        replyToken: event.replyToken,
        messages
      },
      {
        headers: {
          Authorization: `Bearer ${LINE_TOKEN}`,
          'Content-Type': 'application/json'
        }
      }
    );

    res.sendStatus(200);
  } catch (err) {
    console.error('ERROR:', err.response?.data || err.message);
    res.sendStatus(500);
  }
});

/* ===============================
   START SERVER
================================ */
app.listen(PORT, () => {
  console.log(`🚀 Signal Zeeker AI Bot running on port ${PORT}`);
});
