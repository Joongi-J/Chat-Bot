require('dotenv').config();
const express = require('express');
const axios = require('axios');

const app = express();
app.use(express.json());

const PORT = process.env.PORT || 3000;

/* ===============================
   CONFIG
================================ */
const OPENAI_API_KEY = process.env.OPENAI_API_KEY;
const LINE_TOKEN = process.env.LINE_TOKEN;
const FINNHUB_API_KEY = process.env.FINNHUB_API_KEY;

/* ===============================
   SYSTEM PROMPT (วิเคราะห์จริง)
================================ */
const SYSTEM_PROMPT = `
คุณคือ AI นักวิเคราะห์เทคนิคของเพจ Signal Zeeker

หลักการ:
- ใช้ Price Action + Elliott Wave
- วิเคราะห์ตามโครงสร้างราคา ไม่เดา
- ใช้คำว่า "โซน" แทนการชี้นำราคาเป๊ะ
- อธิบายเป็นย่อหน้าแบบบทวิเคราะห์
- ห้ามบอกจุดซื้อขายตรง
- ถ้าข้อมูลไม่พอ ให้บอกตรง ๆ

โครงสร้างคำตอบ:
1) ภาพรวมโครงสร้างราคา
2) Elliott Wave ที่เป็นไปได้
3) แนวรับ / แนวต้าน (เป็นโซน)
4) มุมมองตลาด
5) สรุป
`;

/* ===============================
   Helper: แยกข้อความ LINE (กันหาย 100%)
================================ */
function splitForLine(text, maxLen = 900) {
  const messages = [];
  const paragraphs = text.split(/\n{2,}/).map(p => p.trim()).filter(Boolean);

  for (const p of paragraphs) {
    if (p.length <= maxLen) {
      messages.push({ type: 'text', text: p });
    } else {
      let i = 0;
      while (i < p.length) {
        messages.push({
          type: 'text',
          text: p.substring(i, i + maxLen)
        });
        i += maxLen;
      }
    }
  }

  return messages.slice(0, 8);
}

/* ===============================
   Finnhub: Candle Data
================================ */
async function getCandles(symbol) {
  const to = Math.floor(Date.now() / 1000);
  const from = to - 60 * 60 * 24 * 120; // 120 วัน

  const url = `https://finnhub.io/api/v1/stock/candle?symbol=${symbol}&resolution=D&from=${from}&to=${to}&token=${FINNHUB_API_KEY}`;
  const res = await axios.get(url);

  if (res.data.s !== 'ok') throw new Error('No candle data');
  return res.data;
}

/* ===============================
   วิเคราะห์แนวรับแนวต้าน (ง่ายแต่ใช้จริง)
================================ */
function analyzeSupportResistance(candles) {
  const closes = candles.c;
  const highs = candles.h;
  const lows = candles.l;

  const recentHigh = Math.max(...highs.slice(-20));
  const recentLow = Math.min(...lows.slice(-20));
  const mid = ((recentHigh + recentLow) / 2).toFixed(2);

  return {
    resistance: recentHigh.toFixed(2),
    support: recentLow.toFixed(2),
    mid
  };
}

/* ===============================
   LINE WEBHOOK
================================ */
app.post('/webhook', async (req, res) => {
  try {
    const event = req.body.events?.[0];
    if (!event || event.type !== 'message') return res.sendStatus(200);

    const userText = event.message.text.trim().toUpperCase();
    let replyText = '';

    /* ===== พิมพ์ชื่อหุ้นอย่างเดียว ===== */
    if (/^[A-Z]{1,6}$/.test(userText)) {
      const symbol = userText;

      const candles = await getCandles(symbol);
      const sr = analyzeSupportResistance(candles);
      const currentPrice = candles.c[candles.c.length - 1];

      replyText = `
📊 ${symbol} — ภาพรวมโครงสร้างราคา
ราคาปัจจุบันของ ${symbol} อยู่แถว ${currentPrice} ดอลลาร์ การเคลื่อนไหวในช่วงหลังสะท้อนการแกว่งตัวในกรอบที่ชัดเจน โดยยังไม่เห็นสัญญาณการเบรกโครงสร้างใหญ่แบบชัดเจน

📈 Elliott Wave (เชิงโครงสร้าง)
จากรูปแบบราคา มีความเป็นไปได้ว่าตลาดกำลังอยู่ในช่วงของคลื่นปรับฐาน (Corrective Phase) หลังจากจบคลื่นเร่งตัวก่อนหน้า ลักษณะการแกว่งตัวแบบซ้อนและไม่ทำ Higher High ต่อเนื่อง สอดคล้องกับพฤติกรรมของ Wave 4 หรือโครงสร้าง ABC มากกว่าคลื่นส่ง

📐 แนวรับ / แนวต้าน (Price Action)
แนวต้านเชิงโครงสร้างอยู่บริเวณโซน ${sr.resistance} ดอลลาร์ ซึ่งเป็นบริเวณที่ราคาเคยถูกขายลงหลายครั้ง ขณะที่แนวรับสำคัญอยู่แถว ${sr.support} ดอลลาร์ ซึ่งเป็นฐานของแรงซื้อก่อนหน้าที่ตลาดพยายามปกป้อง ส่วนระดับกึ่งกลางที่ ${sr.mid} ดอลลาร์ เป็นโซนตัดสินใจของแรงซื้อแรงขาย


🧠 มุมมองตลาด
พฤติกรรมราคาช่วงนี้สะท้อนการรอข้อมูลใหม่จากตลาด ไม่ว่าจะเป็นปัจจัยพื้นฐานหรือกระแสเงินทุนจากผู้เล่นรายใหญ่ ทำให้ราคาแกว่งตัวในกรอบมากกว่าการเลือกทิศทางชัดเจน


📌 สรุป Signal Zeeker
โครงสร้างราคายังอยู่ในช่วงสะสมพลัง การดูเพียงราคาปัจจุบันอาจไม่เพียงพอ สิ่งที่ควรจับตาคือการเปลี่ยนแปลงของโครงสร้างและแรงซื้อขายเมื่อราคาเข้าใกล้โซนสำคัญ
`;
    } 
    /* ===== คำถามทั่วไป ===== */
    else {
      const ai = await axios.post(
        'https://api.openai.com/v1/chat/completions',
        {
          model: 'gpt-3.5-turbo',
          messages: [
            { role: 'system', content: SYSTEM_PROMPT },
            { role: 'user', content: userText }
          ],
          max_tokens: 700,
          temperature: 0.6
        },
        {
          headers: {
            Authorization: `Bearer ${OPENAI_API_KEY}`,
            'Content-Type': 'application/json'
          }
        }
      );

      replyText = ai.data.choices[0].message.content;
    }

    /* ===== ส่งกลับ LINE ===== */
    const messages = splitForLine(replyText);

    await axios.post(
      'https://api.line.me/v2/bot/message/reply',
      {
        replyToken: event.replyToken,
        messages
      },
      {
        headers: {
          Authorization: `Bearer ${LINE_TOKEN}`,
          'Content-Type': 'application/json'
        }
      }
    );

    res.sendStatus(200);
  } catch (err) {
    console.error('ERROR:', err.message);
    res.sendStatus(500);
  }
});

/* ===============================
   START SERVER
================================ */
app.listen(PORT, () => {
  console.log(`🚀 Signal Zeeker AI Bot (Elliott + PA) running on port ${PORT}`);
});
